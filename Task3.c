#include <stdio.h>
main ()
{
  int c=3,1;
c = (c * 2 - (1 = 4, --1));
printf("valor de c %d\t velor de 1 %d\n" , c,1); // se impirme el valor de C despues de haber efectuado la operacion propuesta
getch();
return 0;
}